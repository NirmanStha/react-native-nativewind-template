import React from "react";
import { Text, View } from "react-native";

const Setting = () => {
  return (
    <View className="container">
      <Text className="text-red-500">Setting</Text>
    </View>
  );
};

export default Setting;
